/* Welcome to the Interactive C Tutorial.
Start by choosing a chapter and
write your code in this window. */

/*
gcc file.c will compile to an executable in the same directory called a.out
gcc file.c -o file.out will compile that specific file name in the same directory
to run it, use ./a.out or the ./filename.out or pathtodir/filename.out
*/

#include <stdlib.h>
#include <stdio.h>

int main() {
    printf("Hello, World!");
    return 0;
}
